# Crypto Price Analyzer

Готовая программа на Python, которая загружает исторические цены криптовалют с **CoinGecko** и строит таблицы и графики за выбранный период (по умолчанию — 1000 дней).

## Быстрый старт

1) Установите зависимости:
```bash
pip install requests pandas numpy matplotlib xlsxwriter
```

2) Запуск (вариант 1 — с аргументами):
```bash
python crypto_price_analyzer.py --coins BTC,ETH,BNB,SOL,HYPE,XRP,DOGE,TRX,ADA,LINK --days 1000
```

3) Запуск (вариант 2 — через конфиг):
```bash
python crypto_price_analyzer.py --config config.json
```

Файлы результатов появятся в папке `output/`:
- `prices_tidy.csv` — "длинная" таблица (date, symbol, price, return)
- `prices_and_metrics.xlsx` — Excel с вкладками:
  - `Wide_Prices` — "широкая" таблица по датам с колонками `*_price`
  - `Tidy_Prices` — длинная таблица
  - `Metrics` — сводные метрики по каждому коину
  - `Normalized_100` — индексы (старт=100)
- PNG‑графики для каждого коина: цена, лог‑цена, дневная доходность
- `normalized_comparison.png` — сравнение всех коинов на нормированной шкале (старт=100)
- `resolved_mapping.json` — соответствие тикер → CoinGecko ID
- `metrics_summary.csv` — сводка метрик

## Параметры

- `--coins`: список тикеров через запятую (регистр не важен).
- `--days`: количество дней назад (например, 1000, 365). 
- `--out`: папка для сохранения результатов (по умолчанию `output`).

### Замечания
- Источник данных — публичный API CoinGecko (без API‑ключа).
- У некоторых тикеров может быть неоднозначное сопоставление (например, **HYPE**). Скрипт попытается определить автоматически, но при неоднозначности такая монета будет пропущена с предупреждением. При необходимости можно вручную заменить/добавить соответствие в словаре `KNOWN_MAP` в коде.
- Временная зона дат на графиках/в таблицах — Europe/Riga.
- Метрики рассчитываются грубо и предназначены для экспресс‑анализа: CAGR, годовая волатильность, максимальная просадка, «Sharpe‑подобный» коэффициент при безрисковой ставке 0%.

Удачи! 🚀
